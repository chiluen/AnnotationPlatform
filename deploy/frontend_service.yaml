apiVersion: v1
kind: Service
metadata:
   name: frontend-service
   labels:
     frontend: frontend-service
      
spec:
   #type: LoadBalancer
   type: NodePort
   ports:
     - protocol: TCP
       port: 3000
       targetPort: 3000
   selector:
     name: frontend-pod